$(document).ready(function() {
    $("form#ping-pong").submit(function(event) {
        event.preventDefault();
        var result = pingpong
        
        $(".Pingpong").text(number)

        $("#result").show
    });
});

// jQuery user interface logic here
 
var output = function(number) {
    var array = [];
    for (var index = 1; index <= number; index++) {
        if ((index % 15 ) === 0)
    }
}